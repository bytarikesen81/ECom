<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECom"
        tools:targetApi="31">
        <activity
            android:name=".activities.ProfileScreen"
            android:exported="false" />
        <activity
            android:name=".activities.CartScreen"
            android:exported="false"
            android:label="@string/title_activity_cart_screen" />
        <activity
            android:name=".activities.ProductScreen"
            android:exported="true" />
        <activity
            android:name=".activities.ShopScreen"
            android:exported="true" />
        <activity
            android:name=".activities.HomeScreen"
            android:exported="true" />
        <activity
            android:name=".activities.LoginScreen"
            android:exported="true" />
    </application>

</manifest>